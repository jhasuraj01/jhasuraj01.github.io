import { SectionWrapper } from "../../components/section-wrapper/SectionWrapper";

export const SkillGraph = () => {
    return (
        <SectionWrapper>
            <div>Skill Graph</div>
        </SectionWrapper>
    )
}